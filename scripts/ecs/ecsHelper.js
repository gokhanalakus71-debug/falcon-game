function getCondition(entity) {
  return getComponent(entity, "condition");
}

function getStats(entity) {
  return getComponent(entity, "stats");
}

function getTraits(entity) {
  return getComponent(entity, "traits");
}